<div class="row">
  <div class="col s12 m10 offset-m1 l8 offset-l2">
    <div class="card white darken-1">
      <div class="card-content">
        <span class="card-title black-text">Card Title</span>
        <%= form_tag("/users", method: "post", class: "col s12") do %>
          <div class="row">
            <div class="input-field col s6">
              <%= text_field(:user, :first_name, {class: "validate", placeholder: "First Name"}) %>
            </div>
            <div class="input-field col s6">
              <%= text_field(:user, :last_name, {class: "validate", placeholder: "Last Name"}) %>
            </div>
          </div>
          <div class="row">
            <div class="input-field col s12">
              <%= text_field(:user, :username, {class: "validate", placeholder: "Username"}) %>
            </div>
          </div>
          <div class="row">
            <div class="input-field col s12">
              <%= text_field(:user, :email, {class: "validate", placeholder: "Email"}) %>
            </div>
          </div>
          <div class="row">
            <div class="input-field col s12">
              <%= password_field(:user, :password, {class: "validate", placeholder: "Password"}) %>
            </div>
          </div>
          <div class="row">
            <button class="btn waves-effect waves-light" type="submit" name="/users">Sign Up
              <i class="material-icons right">send</i>
            </button>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>