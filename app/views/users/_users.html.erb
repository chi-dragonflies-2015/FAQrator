  <ul class="collapsible" data-collapsible="expandable">

    <% @users.each do |user| %>
    	<li>
    	  <div class="collapsible-header"><%= link_to( user.username , user_path(user), :'data-no-turbolink' => true) %></div>
        <div class="collapsible-body"><p>Topics: <%= user.topics.count %></p></div>
      </li>
    <% end %>

  </ul>
