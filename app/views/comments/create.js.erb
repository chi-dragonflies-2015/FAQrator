console.log("<%= j ( @creator) %>")
$("<%= escape_javascript(render partial: @comment, locals: {creator: false}) %>").appendTo("li#question-<%= @comment.question.id%> ul");
$("li#question-<%= @comment.question.id %> .errors").remove();
$('.collapsible').collapsible({
  accordion : true
});
$(document).find('form').find("input[type=text], textarea").val("");