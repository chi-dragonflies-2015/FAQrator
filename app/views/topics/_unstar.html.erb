<% if current_user.subscriptions %>
<%= form_for(current_user.subscriptions.find_by(topic_id: @topic.id),
             html: { method: :delete }) do |f| %>
  <%= f.submit "Unstar", class: "btn blue lighten-1" %>
<% end %>

<% end %>